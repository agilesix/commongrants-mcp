{
	"name": "commongrants-mcp",
	"version": "1.0.0",
	"private": true,
	"description": "CommonGrants API MCP Server - Progressive OpenAPI exploration and payload validation for CommonGrants protocol",
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"format": "biome format --write",
		"lint:fix": "biome lint --fix",
		"start": "wrangler dev",
		"cf-typegen": "wrangler types",
		"type-check": "tsc --noEmit",
		"test": "vitest run",
		"test:watch": "vitest watch",
		"test:unit": "vitest run test/unit",
		"test:integration": "vitest run --config vitest.integration.config.ts",
		"test:integration:watch": "vitest watch --config vitest.integration.config.ts",
		"test:all": "npm run test:unit && npm run test:integration",
		"test:coverage": "vitest run --coverage",
		"test:coverage:unit": "vitest run --coverage test/unit",
		"test:coverage:integration": "vitest run --config vitest.integration.config.ts --coverage"
	},
	"dependencies": {
		"@modelcontextprotocol/sdk": "1.19.1",
		"@scalar/openapi-parser": "^0.22.3",
		"agents": "^0.2.8",
		"js-yaml": "^4.1.0",
		"zod": "^3.25.76"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.2.5",
		"@cloudflare/vitest-pool-workers": "^0.9.14",
		"@types/js-yaml": "^4.0.9",
		"@types/node": "^24.9.1",
		"@vitest/coverage-v8": "^3.2.4",
		"typescript": "5.9.3",
		"vitest": "^3.2.4",
		"vitest-fetch-mock": "^0.4.5",
		"wrangler": "^4.44.0"
	}
}
